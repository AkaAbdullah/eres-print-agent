# Runtime dependencies. Prefer `pip install -e .` (uses pyproject.toml) for
# development — this file is here for environments that want a plain
# `pip install -r requirements.txt` (e.g. the Windows build machine, before
# PyInstaller freezes everything into a single exe).
pydantic>=2.6,<3
websockets>=12,<13
keyring>=24,<26
httpx>=0.27,<1

# Windows-only: Windows Service integration (win32serviceutil) and native
# printer discovery/printing (win32print). Not installable on macOS/Linux —
# see printing/mock.py and printing/factory.py for how the codebase stays
# importable without it on non-Windows platforms.
pywin32>=306; sys_platform == "win32"
